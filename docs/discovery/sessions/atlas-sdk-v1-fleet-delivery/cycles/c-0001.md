---
schema-version: 1
cycle: c-0001
session: atlas-sdk-v1-fleet-delivery
started: 2026-08-21T15:58:54-04:00
completed: 2026-08-21T16:26:00-04:00
anchor-revision: 2026-08-20T15:57:19Z
anchor-status: unchanged
entry-state-digest: cf820c5337664c6a207e7639027968186ac84a4484bf3c2908e88c7b50048e84
exit-state-digest: 8b85b09e3f1b82700ae32a62ac0beb5e569eb1c3ef4732177fc904696d2f86ec
entry-root-map-digest: 311f1bff2ed230238f335a6b3faea8ea52b1366782a03770e4734e70896bc53a
exit-root-map-digest: 0c0c1398b8e4864a98105625d5d50c99f1913b1c8f9b11d02f3316ccc2096e3a
entry-root-lexicon-digest: 07c02c1eeba6d724248eafdee49c70d21ed307a6e59cd6d9254cf4a22d7593f0
exit-root-lexicon-digest: ab4902ed0f6ed6bc60b105873b58c58f998bc51920fd58dd315f1add5fe39f93
digest-tool: shasum -a 256
digest-status: verified
question-group-size: 12
questions-asked: 2+0
selection-source: deterministic
traversal-mode: deep
promotion: not-ready
promotion-identity: none
domain-handoff: atlas-sdk-v1-fleet-delivery/c-0001/fleet-term-collision
domain-handoff-status: pending
domain-handoff-pending: 1 (atlas-sdk-v1-fleet-delivery/c-0001/fleet-term-collision)
tracker-mode: remote
state-scope: full
outcome: CYCLE_ADVANCED
composition-report: discovery=available; create-ticket=available; interrogate=available; domain-mapping=available-changed; spec=available-changed
---

# Cycle c-0001

## Selection

Selected **n-0004 - Legacy-vocabulary containment**, traversal mode `deep`,
`selection-source: deterministic`.

Rules 1 through 3 produced no candidate: no reinterpretation verdict was
`invalidated` or `orphaned`; every node was `unprioritized`, and unprioritized
nodes generate no debt against each other; and no blocked node had two or more
dependents. **Rule 4, high risk or major uncertainty, fired.**

n-0004 was the only high-risk flag. The flag is auditable rather than asserted:
n-0003 carries an equally documented failure - pull request #113 breaking `main`
with no merge conflict - but issue #112 also records its mitigation. n-0004 has
a documented failure with **no** recorded mitigation, and its failure mode
passes `npm run ci` silently. Silent and gate-passing outranks documented and
mitigated.

This was the session's first cycle, so the tree was seeded this cycle from the
anchor and the closed corpus.

## Reinterpretation

No verdict changed. Every node is stamped `reinterpreted: c-0001 (intact)` by
construction, because the tree was created this cycle. This is explicitly **not**
an independent reinterpretation pass, and cycle c-0002 will perform the first
real one against issue #112.

## Questions and Answers

### Q1 (grounded) - Does extending the vocabulary gate to every authored surface block opening the phase 5-12 lanes?

Recommended yes, with #117 as lane zero, widening `vocabulary:validate` to
`scripts/**/*.ts`, `docs/agents/atlas-sdk/**`, and any `.atlas/` templates the
Framework Bundle ships. Rationale: both renames drifted past a green
`npm run ci` under a single author, and evidence R1 shows remediation cost is
zero today. Alternative offered: open lanes now and rely on the Dragon Council
plus Maintainer review, treating #117 as ordinary backlog, at the cost of a
growing remediation surface.

Three answers were received across three turns, and the record keeps all three
because two were not decisions:

1. "Continue with implementation." **Ambiguous, not recorded as an answer.** The
   phrase could mean accept-and-proceed or exit-discovery-and-implement, and
   implementation is outside this loop's scope under either reading.
2. "we can do discovery... let me know how i can help." **Process clarification,
   not an answer.** It withdrew the implementation reading and delegated process,
   not the decision. Q1 was re-put unchanged and did not re-spend budget.
3. "sounds good." **Settled - recommendation accepted.** Read as acceptance of a
   question's recommendation, not as an approval gate; no gate was pending. The
   reading was stated back in the same turn for correction.

### Q2 (grounded) - How may a sub-agent consume the closed corpus?

Recommended fence and extract: treat the 27 closed decision tickets as immutable
historical provenance, never as agent-facing instruction, and carry their
substance into in-repo current-vocabulary requirements. Alternative offered:
annotate each closed issue with a rename banner and keep pointing agents at the
corpus, which is far cheaper and preserves one source of truth, but depends on
every agent correctly applying a sixteen-row mapping table while reading and
cannot fix decisions that are wrong rather than merely old-spelled.

Answer: "fence and extract." **Settled - recommendation accepted**, in the
Maintainer's own words.

The group closed at 2 of 12 under the rule "the material fog for the selected
node is resolved", not by reaching the cap. **Ten questions were left unused
deliberately.** Unused grounded capacity was not converted into follow-ups.

## Decisions

| # | Decision | Owner | Nodes |
| --- | --- | --- | --- |
| D1 | Extending the vocabulary gate to every authored surface blocks opening the phase 5-12 lanes. Issue #117 is lane zero. | Maintainer | n-0004, n-0008 |
| D2 | The 27 closed decision tickets are immutable historical provenance, not agent-facing instruction. Their substance is extracted into in-repo current-vocabulary requirements, and each `ready-for-agent` ticket is made self-sufficient. | Maintainer | n-0004, n-0009 |

D2 has a consequence beyond its own wording: any conflict between a corpus entry
and merged code now resolves in favor of merged code. The first known such
conflict is recorded as evidence R3.

## Evidence

Three read-only findings were gathered **before** the question group, so the
Maintainer's budget was spent on judgment rather than lookup:

- **R1** - whole-word sweep for eleven retired terms across `scripts/`,
  `.cacophony/`, `docs/agents/`, `.agents/`, and `.github/` found **zero**
  product-term occurrences. The only hits are ordinary English section headings.
  The gap issue #117 describes is real but currently unexploited, so remediation
  cost is zero today. Limitation: whole-word only; a term inside a compound
  identifier or split across a line break would not match.
- **R2** - `docs/ci.md` states the vocabulary gate scans `src/**/*.ts` only, and
  itself names `scripts/atlas_sdk_agents.ts` and
  `docs/agents/atlas-sdk/personas/` as unscanned surfaces that carry Core
  Archetype terms into user Atlases.
- **R3** - issue #6 retires the `lint` alias "before implementation", while
  `atlas lint --machine` is shipped and **Lint** is the canonical `CONTEXT.md`
  term. A sub-agent faithfully executing #6 would rename a merged, tested, gated
  command. This is the finding that separated "annotate" from "fence" in Q2.

Also imported this cycle, under the legacy-artifact rule: issue #11 and its 27
indexed decisions, as historical provenance, with the originals unmodified.

## Lexicon Changes

- `Fleet` added as **conflicted**, with both readings named and sourced. Blocks
  promotion of n-0004. No confirmed row was written and no confirmed-section
  mirror was made, because no domain skill confirmed it.
- `Lane`, `Lane zero`, `Phase`, `Tracer-bullet ticket`, and `Merge trap` added to
  the shared lexicon as **candidate**.
- `Legacy corpus` added to the session lexicon as **candidate**.
- No term reached `confirmed` this cycle. The one material handoff is
  `pending`, so nothing was mirrored from a canonical artifact.

## Promotion Outcome

`not-ready`. No preview was rendered, no approval was sought, and **nothing was
published to the tracker**.

The subtree fails the readiness gate on four independent counts: n-0004 is fog
`researched` and maturity `researched` where promotion needs fog `cleared` and
maturity `promotion-ready`; n-0004 depends on the `conflicted` term `Fleet`,
which blocks promotion by itself; n-0008 and n-0009 were created this cycle at
maturity `framed`; and `tracker-tier-map` is `unmapped`, which would require
`Approve tier map atlas-sdk-v1-fleet-delivery` before any preview.

## State Delta

- Session package created at `docs/discovery/sessions/atlas-sdk-v1-fleet-delivery/`
  with the state root and both root files, under `Approve session setup`.
- Tree seeded with eight nodes; two more created from this cycle's decisions, for
  ten total.
- n-0004 advanced from fog `unexplored` to `researched`, traversing `scouted` and
  `investigating` rather than skipping, and from maturity `vague` to
  `researched`. Priority set to `P0`.
- n-0008 and n-0009 created at priority `P0`, fog `scouted`, maturity `framed`.
- n-0002's dependency moved from n-0004 to n-0009.
- Requirements gained decisions D1 and D2, unresolved items U8 and U9, and a
  revised U4. Evidence gained R1, R2, and R3, plus the corpus import record.
- Priority debt: **none**. Only n-0004 advanced in maturity and it is P0; the two
  created nodes are themselves P0; a pairing at equal priority generates no debt;
  and every remaining node is `unprioritized`.

## Limitations

- **The one material domain handoff was not invoked.** `/domain-mapping`
  resolves with matching identity, but this repository ships its own
  `/domain-modeling` skill claiming the same canonical artifacts named by
  `docs/agents/domain.md`. Two skills owning one artifact set is an ownership
  question the Maintainer owns; invoking either would have settled it silently.
  Recorded `pending`; `Fleet` stays `conflicted`; promotion is blocked for
  n-0004 only.
- 23 of the 27 closed decision tickets were read only through issue #11's
  one-line summaries. Full bodies unread. R3 found one contradiction by targeted
  comparison, not exhaustive audit; more may exist.
- Issue #112's claim that the continuous integration council is offline is
  unverified against recent workflow runs. It directly affects n-0005.
- No measurement exists for concurrent agent load, review latency, or merge
  contention. Every throughput statement in this session is inference.
- `composition-report` records two `available-changed` entries: the domain skill
  identity above, and `/spec`, because this repository owns its own `/spec` and
  `/tickets` chain while this loop's invariant routes every tracker mutation
  through `/discovery`. That conflict is unresolved and will matter at the first
  real promotion.

## Next Frontier

Deterministic next selection for cycle c-0002, advisory only:

Rule 1 is empty. Rule 2, priority debt, is empty. Rule 3, shared blocker,
**fires**: n-0004 now blocks both n-0008 and n-0009, giving it two dependents.
But n-0004's material fog is resolved, so the useful reading of rule 3 points at
its blocked children.

Applying the tie-breakers among n-0008 and n-0009 - equal priority `P0`, equal
maturity `framed`, then greater dependent count - **n-0009 has one dependent
(n-0002) and n-0008 has one dependent (n-0001)**, so the tie falls to earlier
first-seen, which is equal, and then to lowest node id: **n-0008**.

Recommendation for c-0002: **n-0008 - Lane zero**, deep. Its three open
questions - exact surface scope, whether Framework Bundle templates count as
authored, and the adversarial corpus case that proves the widened gate - are the
last thing between a settled decision and an executable ticket.

The unresolved `Fleet` handoff is carried forward and remains eligible for
selection as this session's next domain packet.
